# Generic small-scale run (the safe fallback). Pick a task-specific profile
# (smoke / lora / sft / dpo / grpo / pretrain) when one fits.
max_paths: 2
max_retries_per_path: 2
compute_cap_gpu_h: 2.0
scale_ceiling_params: 200000000
token_budget: null

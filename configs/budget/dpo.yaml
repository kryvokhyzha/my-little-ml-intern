# Preference tuning (DPO). A frozen reference model doubles the forward pass, so
# more GPU-h than SFT; LoRA-DPO keeps the big-base ceiling.
max_paths: 2
max_retries_per_path: 2
compute_cap_gpu_h: 8.0
scale_ceiling_params: 12000000000
token_budget: null

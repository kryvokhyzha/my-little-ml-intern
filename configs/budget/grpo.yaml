# GRPO / online RL. Generation-bound (rollouts dominate wall-clock), so the
# highest GPU-h cap; fewer retries because each path is expensive to repeat.
max_paths: 3
max_retries_per_path: 1
compute_cap_gpu_h: 12.0
scale_ceiling_params: 12000000000
token_budget: null

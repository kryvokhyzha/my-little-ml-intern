# Full-parameter SFT. Memory-bound, so the param ceiling is lower than LoRA's but
# the wall-clock is higher (no frozen base, full optimizer state).
max_paths: 2
max_retries_per_path: 2
compute_cap_gpu_h: 6.0
scale_ceiling_params: 3000000000
token_budget: null
